name: api_client
description: Generic HTTP transport, request policy, and DataGateway. No feature paths.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  http: ^1.1.0
  app_result:
    path: ../app_result
  app_connectivity:
    path: ../connectivity

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
