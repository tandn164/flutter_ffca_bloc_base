name: app_push
description: Provider-independent push contracts and message parsing. No GoRouter or app routes.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0
resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
