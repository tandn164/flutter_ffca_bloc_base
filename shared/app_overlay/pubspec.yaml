name: app_overlay
description: App-lifetime loading, toast, offline policy, and tutorial host for Flutter apps.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0
  flutter: ">=3.16.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  app_connectivity:
    path: ../connectivity
  tutorial_engine:
    path: ../tutorial_engine

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
