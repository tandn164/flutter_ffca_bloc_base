name: composable_network
description: ComposableCore — API utilities, connectivity stream, safe JSON decode.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  composable_core:
    path: ../composable_core
  get_it: ^8.0.3
  internet_connection_checker: ^3.0.1
  meta: ^1.9.1
  
  # HTTP Client
  chopper: ^8.3.0
  http: ^1.6.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
  chopper_generator: ^8.3.0
  build_runner: ^2.15.0
