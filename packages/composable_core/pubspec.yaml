name: composable_core
description: ComposableCore — config loader, module contract, bootstrap.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  get_it: ^8.0.3
  meta: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
