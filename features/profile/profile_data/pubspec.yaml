name: profile_data
description: Profile data — ProfileApi (/demo/me), DTOs, repository impl.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  chopper: ^8.1.0
  api_client:
    path: ../../../shared/api_client
  app_result:
    path: ../../../shared/app_result
  profile_domain:
    path: ../profile_domain

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
  build_runner: ^2.4.13
  chopper_generator: ^8.1.0
