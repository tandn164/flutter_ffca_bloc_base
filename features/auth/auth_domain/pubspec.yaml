name: auth_domain
description: Auth credentials domain — TokenPair, AuthRepository, login/signup use cases.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  app_result:
    path: ../../../shared/app_result

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
