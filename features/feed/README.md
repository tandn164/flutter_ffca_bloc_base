# Feed Feature

A reusable paginated task/feed example split into `feed_domain`, `feed_data`,
and `feed_presentation`. It demonstrates cache-aware reads, optimistic UI, and
idempotent offline mutation retries without coupling the feature to an app.

See each package README and
`apps/sample_app/lib/app/features/feed_feature.dart` for composition.
