name: sample_domain
description: Sample (todo list) domain — entities, repository contract, use cases.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  freezed_annotation: ^3.1.0
  app_result:
    path: ../../../shared/app_result

dev_dependencies:
  build_runner: ^2.4.13
  freezed: 3.0.6
  lints: ^6.0.0
  test: ^1.25.0
