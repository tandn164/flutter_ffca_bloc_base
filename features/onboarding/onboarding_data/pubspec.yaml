name: onboarding_data
description: Local-storage implementation of onboarding completion state.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  local_storage:
    path: ../../../shared/local_storage/core
  onboarding_domain:
    path: ../onboarding_domain

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
